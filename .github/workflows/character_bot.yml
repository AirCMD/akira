name: Local Character Bot

on:
  issues:
    types: [opened, reopened]
  issue_comment:
    types: [created]

jobs:
  react-without-api:
    if: github.event.comment.user.type != 'Bot' && github.event.issue.user.type != 'Bot'
    runs-on: ubuntu-latest
    
    permissions:
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests

- name: Run Character Bot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          COMMENT_BODY: ${{ github.event.comment.body || github.event.issue.body }}
          ISSUE_AUTHOR: ${{ github.event.comment.user.login || github.event.issue.user.login }}
        run: |
          python bot/bot.py
